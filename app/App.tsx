import { StatusBar } from "expo-status-bar";
import { StyleSheet, Text, View } from "react-native";

export default function App() {
    return (
        <View className="flex h-full justify-center items-center bg-gray-200">
            <Text>Home Screen</Text>
        </View>
    );
}
